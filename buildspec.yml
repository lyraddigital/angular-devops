version: 0.2

phases:
  install:
    commands:
    # Install Angular CLI
      - npm i -g @angular/cli@6.2.4
    # Install project dependencies
      - npm i
  build:
    commands:
    # Build the project
      - ng build --production

artifacts:
  files:
    - '**/*'
  discard-paths: yes
  base-directory: 'dist*'